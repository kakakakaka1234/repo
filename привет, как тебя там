def who_are_you_and_hello():
    while True:
        name = input()
        if name.isalpha() and name[0].isupper() and name[1:].islower() and len(name.split()) == 1:
            print(f"Привет, {name}!")
            break
